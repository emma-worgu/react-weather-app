import React from 'react';
import './loading.css';

function Loading() {
    return (
        <div>
            <h4>Please Wait why we get the information...</h4>
        </div>
    );
};

export default Loading;